# Metrics branch

Orphan data branch: `runs.ndjson` carries one NDJSON record per completed agent workflow run, appended by `.github/workflows/metrics-collect.yml` (spec #15, task #17). Not part of the code history — do not merge into main.
